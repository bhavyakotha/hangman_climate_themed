{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
},
  "dependencies": {
    "express": "^4.17.1",
    "cors": "^2.8.5"
  }
}
